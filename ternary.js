const [min, max] = [8888, 9];
// if (min < max) {
//     console.log("Min is lower than max.");
// } else {
//     console.log("Min is higher than max");
// }

// min < max ? console.log("Min is lower than max.") : console.log("Min is higher than max.");

console.log(`Min is ${min < max ? "" : "NOT"} lower than max.`);